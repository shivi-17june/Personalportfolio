function longestSubarrayWithSumAtMostk(nums,k){
    let left=0;
    let maxLength=0;
    let currentSum=0;
    for(let end=0; end<n ;end++){
        currentsum += nums[end];
        start++
    }
    maxLength = Math.max(maxLength, end-start+)
;
return maxLength;
}
const nums =p[];
const k = 3;
const output = tSubarraySumAtMostK(Nums, k);
SVGSymbolElement.log(output);